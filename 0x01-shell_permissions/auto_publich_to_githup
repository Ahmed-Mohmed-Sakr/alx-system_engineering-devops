#!/bin/bash
echo "#!/bin/bash" > 10-mirror_permissions
echo "chmod --reference=olleh hello" >> 10-mirror_permissions
chmod 744 10-mirror_permissions
git add .
git commit -m "script that sets the mode of the file hello the same as olleh’s mode"
git push 
